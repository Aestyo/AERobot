
module.exports = async (client, channel, user) => {
    //client.log(`Un utilisateur a commencé à écrire : "${user.username}" sur le channel "${channel.name}" sur le serveur  "${channel.guild.name}"`, `rec`, -1);
}