module.exports.run = async (client, message) => {
  /*while(true){
    await new Promise((resolve) => setTimeout(resolve, 1000));
    console.log(`${client.time.hours}:${client.time.minutes}:${client.time.seconds}`);  
  }*/
};
  